import { Routes } from '@angular/router';
import { InstrumentsPageComponent } from './instruments/instruments-page.component';

export const routes: Routes = [{ path: '', component: InstrumentsPageComponent, title: 'degen' }];
